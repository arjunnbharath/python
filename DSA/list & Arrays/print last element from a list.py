# Given:
numbers = [10, 20, 30, 40, 50]
#Print the first and last elements.

last_element = len(numbers)-1
print(numbers[last_element])

# or we can use the simple way

element_last = numbers[-1]
print("simple way :", element_last)